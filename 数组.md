1.二分查找：通过与中间数字对比大小，寻找特定值。注意边界条件开闭
2.移除val元素（要求只能在当前数组操作，不需要考虑新数组的后面数字大小）：
  最优解：双指针，第一个指针去寻找非val的元素，第二个指针跟新，遇到val保持不动，最终就可以返回新数组的大小
  暴力：双循环：遍历熟组，寻找到val,进入第二层循环，跟新val与后面的值
(指针常量：指针是常量，指向不可改，值可以
 常量指针：指向常量的指针，指向可以改，值不可以)
3.有序数组的平方从新排序：
  暴力：平方后快排；
  双指针：新建一个数组，在原始数组一头一尾插入指针，越靠边缘，绝对值越大，对比后直接插入新数组末端。循环最后要用i<=j,因为最后的一个nums[i/j],要放入新数组。
4.长度最小子数列：滑动窗口，先滑动末端窗口至大于等于x,然后移动初始窗口，对比双for循环做了剪枝。要注意result=INT_MAX，因为我们会用到min（result，j-i=1
5.螺旋矩阵：2222222
  